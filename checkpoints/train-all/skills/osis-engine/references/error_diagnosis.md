# 错误诊断参考表

> OSIS 报错的总索引;每个错误的具体修复方法由对应模块 SKILL 提供。

## 报错 → 根因 → 解决

| 报错特征 | 根因 | 解决方向 |
|---|---|---|
| `Uz >> 1e5` / 某方向位移超大 | 边界 0/1 写反,某自由度本应约束却释放 | 检查所有 `create_general` 参数 —— **不写 = 约束(1),写 0 = 释放**。重点查单梁 Ry/Rw |
| `Uz >> 1e5` / Stage 1 简支梁节点位移异常 | 临时支座设置在当前阶段尚未激活的节点或单元范围外,导致阶段结构端点变成悬臂 | 临时支座应放在该阶段实际活动构件的端点节点上,例如"预制梁端节点"而非"现浇段节点",并与阶段激活的单元组一致 |
| `Ry自由度位移=1e10` | 单梁模型未约束绕 Y 轴转动 | 至少一个支座保留 bRY=1(不传该参数) |
| `组不存在` / `load group not found` | `_6` 的组名与 `_8`/`_9`/`_10` 引用的名字不一致 | 对比三处引用,改到逐字一致(含中文/下划线/数字) |
| `夹角大于90度` | 钢束控制点排布使弧线出现折返 | 用 4 点 ARC3D,不要 6 点;首尾 r=0,中部 r=10~75 |
| `形状控制点坐标超出参照单元组的坐标范围` | 钢束曲线的 x 范围超出了 `element_group` 覆盖的节点 x 范围 | 检查 `create_arc3d` 的 x 坐标是否在单元组首尾节点 x 之间 |
| `shape.layout('GLOBAL')` 报错 / 预应力工况需张拉/钝化的预应力所在的单元尚未激活 | 钢束曲线终点或投影组跨越了当前阶段尚未激活的单元 | 确保钢束线型端点和投影单元组都限制在当前阶段已激活的单元范围内,必要时同步缩回到当前阶段的活动单元组 |
| `工况结果不存在,请先求解` | 施工阶段分析的结果不能按单独工况名导出 | 调 `osis-check` 做组合验算,不要直接 `result.loadcase()` |
| `StgLC,...` / `该工况中不存在名为 xxx 的预应力板束(或荷载)` | `_10` 的 `define_loadcase` 引用了 `_8` 未创建或拼写不一致的工况名;或误把钢束 shape 名(如 `N1-1`)当工况名引用 | 打开 `_8_loadcase.py` 逐条 `load.create` 对照,把 `_10` 的引用名改到逐字一致(只改一侧);改后**单模块验证 `_10_stage.py`**,禁止不改代码重跑 `main.py` |
| `Section,1,xxx...` 参数错误 | 截面 API 参数数量/顺序不匹配 | 回读 `osis-module-section` 确认各参数含义 |
| `该参数组合几何检查不通过` | 用默认尺寸但桥型缩小,参数不自洽 | 从目标 h/bt/bb 反推全部几何参数,不可只改一处 |
| `弹性连接报错` | 弹性连接单元 nBirth 设了 3.0 | 弹性连接必须 `nBirth=0.0`,混凝土单元才用 nBirth≥3.0 |
| `非法输入：{64}` / 命令流 `AsgnBd,n,a,{64}` | `boundary.assign("a", {64})` 把 Python set 写进命令 | 改成 `assign("a", 64)` 或 `"2to4"` / list;禁止 set |
| `Solve` / `CS1_0号段施工：未定义边界条件,或者边界条件未定义在已激活单元上` | 0 号块已激活,墩顶临时支架没挂上(set 写法失败,或支架节点不在本阶段单元上) | 先修 `_7` 的 assign;CS1 必须激活「桥墩临时支架」且支架在墩顶主梁节点 |
| `同一阶段内不允许重复激活单元组` | 未 clear 时 L1 重跑 `_10`,`define_element` 把已激活的组又加一次 | 改代码后 L2,或先删该阶段再 L1 |
| `add_rebar_s` / `RebarS` 无报错但箍筋没变 | 截面上已有同类型 ShearStirrup,二次 add HTTP 成功但模型不更新 | 先 `delete_rebar_s("ShearStirrup")` 再 add;用 `section.get(no).rebar.has_shear_stirrup` 读回 |
| `ImportError: cannot import name 'text_encoding' from 'io'` | 没用会话里的 `python`(PATH 上已配好) | 用 `python`,不要用系统旧解释器 |
| `ModuleNotFoundError: No module named 'pyosis'` | 没用会话里的 `python` | 用 `python`(已装 pyosis);装库 `python -m pip` |
| `AssertionError: SRE module mismatch` | Python 多版本冲突 | 用 `python`,不要另开系统解释器 |

## 经验总结(高频踩坑)

1. **临时支座必须放在当前阶段实际激活的构件端点上**。如果临时支座设置在未激活单元范围外,当前阶段结构可能出现悬臂,导致 Uz 位移异常。

2. **钢束投影单元组必须覆盖钢束控制点的完整 x 范围**。如果组范围不足,`shape.layout('GLOBAL')` 会报"控制点坐标超越参照单元组的坐标范围"。

3. **预应力张拉/钝化阶段的钢束必须完全位于当前阶段已激活的单元范围内**,线型端点和投影组范围必须同步,否则会出现"钢束进入未激活单元"或求解时局部失稳。

4. **检查时三步走**:
   - 阶段激活了哪些单元组?
   - 临时支座在哪个节点?这个节点是否在当前阶段激活的单元上?
   - 钢束曲线 x 范围 + 投影组是否都落在已激活单元范围内?

5. **跨模块组名同步**:`_6` 的组名是跨模块契约。任一处差一个字,`_8`/`_9`/`_10` 报"组不存在"。

6. **工况名也是跨模块契约**:`_8` 的 `load.create(<名>, ...)` 工况名是 `_10` 的 `define_loadcase(..., <名>)` 的唯一合法引用源。工况名 ≠ 钢束 shape 名——`_10` 只引用工况名;写 `_10` 前打开 `_8_loadcase.py` 逐条对照,不凭记忆写。

7. **失败修复的唯一链路是"改代码 → 单模块验证 → 全量验证"**。未修改 `.py` 就重跑 `main.py`(它先 `clear()` 清空全模型,一次数分钟)必然在同一处再次报错,是"报错 → 全量重建 → 同一报错"死循环的唯一来源。每次全量重建前,必须能说出本次代码 diff 及其与报错的关系;同一报错出现第 2 次,停止重跑并向用户汇报。
