# OSIS-AI 与主流智能体框架的全任务线对比实验框架

文档版本：v1.0  
日期：2026-09-02  
适用范围：桥梁设计工作流的五条任务线  
实验目录：`<comparison-repo>`  
被测项目：`<path-to-osis-skill-enhance-main>`

---

## 1. 实验目标与范围

本实验比较 OSIS-AI（T6）与五类代表性智能体架构（T1–T5）在桥梁设计工作流上的端到端能力。五条任务线为：

1. 建模；
2. 验算修复；
3. 结果分析；
4. 计算书生成；
5. API 问答。

每条任务线单独评分，再进行跨任务线汇总。耗时和资源消耗是所有任务线通用的指标，其余指标按任务线定义。

端到端流程为：

    统一任务输入
        ↓
    T1–T6 智能体编排与工具调用
        ↓
    统一技能/文档只读挂载 + 候选工程
        ↓
    任务线专用执行器（建模线为 PyOSIS）
        ↓
    自动验证
        ↓
    统一评价与可追溯产物

本实验回答：

- 不同智能体编排方式对五条任务线的成功率和质量有什么影响？
- OSIS 的技能分层、案例路由、状态化模型、验证器和增量重跑分别带来多少收益？
- T6 的优势主要出现在哪条任务线、哪个阶段？
- T6 的优势是否来自架构本身，而不是技能、模型、模板、工具或求解器差异？

本阶段不比较聊天界面，不纳入通用代码编辑任务、计算书排版之外的办公任务和人工操作体验。

---

## 2. 五条任务线

| 任务线 | 输入 | 主要动作 | 主要输出 | 主要正确性判据 |
|---|---|---|---|---|
| 建模 | 自然语言需求、桥型、参数、技能/案例 | 生成或修改候选工程，由统一 PyOSIS 真实建模，再调已有 `model_conformance` CLI | 候选工程、`model_state.json`、CLI 报告、执行日志 | 软件实际构造出的模型参数；求解仅作可选 gate |
| 验算修复 | 已建模型、含 NG 的验算结果、问题说明 | 定位 NG 原因，修改模型或参数，重新验算 | 修复补丁、前后快照、验算日志 | NG 定位准确且修复后通过 |
| 结果分析 | 已求解模型、结果文件、工程问题 | 提取数值并做工程判断 | 结构化答案、引用位置、判断依据 | 数值、单位、工况和判断正确 |
| 计算书生成 | 计算书模板、项目数据、模型/结果 | 解析模板并填充完整计算书 | DOCX/PDF、字段映射、生成日志 | 字段准确、结构还原、数模一致 |
| API 问答 | PYOSIS 接口文档、模板、用户问题 | 回答接口用法、参数和模板问题 | 文本答案、接口引用、示例代码 | 接口签名正确、无幻觉、响应及时 |

### 2.1 建模任务线

建模线覆盖整桥生成、单模块生成和基于已有快照的模块修改，桥型包括：

- cantilever_box；
- conventional_box；
- hollow_slab；
- precast_small_box；
- precast_t_girder；
- rigid_frame。

所有架构最终必须输出：

    candidate_project/
    └─ py/
       ├─ 项目画像.md
       └─ prep/
          ├─ main.py
          ├─ _0_engine.py
          ├─ _1_control.py
          ├─ _2_property.py
          ├─ _3_material.py
          ├─ _4_section.py
          ├─ _5_node.py
          ├─ _6_element.py
          ├─ _7_boundary.py
          ├─ _8_loadcase.py
          ├─ _9_analysis.py
          └─ _10_stage.py

静态代码评价与真实 PyOSIS 评价必须分开：CLI 评分读取候选 Python 工程；`model_state.json` 必须来自 `OSISEngine.model_summary()`。正式建模主分数不由求解收敛替代。

### 2.2 验算修复任务线

每个任务提供已建桥梁和一个或多个 NG 项。候选系统必须读取验算结果、输出原因定位、修改目标模块或参数、重新执行验算并保存前后差异。不能只看补丁是否编译。

### 2.3 结果分析任务线

问题可以是最大挠度、控制工况、支反力分布、超限判断和关键施工阶段。答案必须包含数值、单位、工况/阶段和结果来源。

### 2.4 计算书生成任务线

候选系统接收模板和项目数据，生成完整计算书。必须检查字段、章节、表格、渲染结果、缺失数据标记，以及计算书数值与模型实际结果的一致性。

### 2.5 API 问答任务线

问题来自真实 PYOSIS 接口、参数签名、模块用法和模板约定。不存在的接口、参数或返回值计为幻觉。

---

## 3. 参照架构与版本锁定

| 编号 | 架构 | 推荐实现/版本 | 动作形式 | 拓扑/规模 | 知识挂载 |
|---|---|---|---|---|---|
| T1 | 直接执行 | Liang et al. 2504.09754；FEABench 作为任务参考 | 一次性生成 | 单智能体、无循环 | 固定技能索引和文档包 |
| T2 | ReAct | LangGraph 1.2.11 + LangChain 1.3.18 | JSON 工具调用 | 单智能体、循环 | 统一只读技能和案例工具 |
| T3 | CodeAct 简单框架 | smolagents 1.26.0 | 代码即动作 | 单智能体、轻量循环 | instructions、prompt template、自定义工具 |
| T4 | CodeAct 完整平台 | OpenHands SDK 1.44.1 | 代码即动作 | 单智能体、完整工作区 | 原生 .agents/skills 与 SKILL.md |
| T5 | 角色多智能体 | CrewAI 1.15.18 | 工具调用与角色协作 | 多智能体 | 主轨统一工具；副轨可用 knowledge sources |
| T6 | OSIS-AI 全量系统 | 正式实验前建立干净 OSIS 快照 | 原生路由/代码动作 | 状态化、增量式 | 挂载与 T1–T5 相同的 `checkpoints/train-all/skills` 快照（由适配器复制到本次隔离项目） |

正式运行前冻结并记录框架、Python、模型、OSIS/PYOSIS、求解器和容器版本，同时记录 OSIS commit、技能包 SHA-256、工具 schema 版本和运行环境 digest。升级任何关键依赖都必须开启新的实验批次。

---

## 4. 总体系统架构

    任务数据集与输入校验
              ↓
    统一框架适配层（T1–T6）
              ↓
    统一技能/案例/文档访问层
              ↓
    统一 OSIS、文件、计算书和结果工具层
              ↓
    任务线执行器
              ↓
    任务线专属评价器 + 通用耗时/资源评价
              ↓
    统一 manifest、报告和统计分析

推荐目录：

    <comparison-repo>\
    ├─ common\
    │  ├─ task_schema.py
    │  ├─ skill_adapter.py
    │  ├─ modeling_pipeline.py
    │  ├─ osis_adapter.py
    │  ├─ static_conformance.py
    │  ├─ evaluator.py
    │  └─ runner.py
    ├─ baselines\
    │  ├─ t1_direct\
    │  ├─ t2_langgraph\
    │  ├─ t3_smolagents\
    │  ├─ t4_openhands\
    │  └─ t5_crewai\
    ├─ baselines\t6_osisai\
    ├─ tasks\
    │  ├─ modeling\
    │  ├─ verification_repair\
    │  ├─ result_analysis\
    │  ├─ calcbook\
    │  ├─ api_qa\
    │  ├─ dev\
    │  └─ hidden\
    ├─ runs\
    ├─ reports\
    └─ manifests\

---

## 5. 统一任务协议

每个任务 JSON 至少包含：

    {
      "task_id": "...",
      "task_line": "modeling",
      "task_form": "whole",
      "difficulty": "L1",
      "natural_language_requirement": "...",
      "input_artifacts": [],
      "initial_project_snapshot": null,
      "expected_outputs": [],
      "expected_fields": {},
      "metadata": {
        "split": "dev",
        "solver_required": true,
        "seed_group": "default"
      }
    }

task_line 必须显式给出；任务线专属数据放在 input_artifacts、expected_outputs 和 expected_fields 中。

统一适配器语义：

    generate(task, skill_reader, workspace) -> Path
    repair(task, skill_reader, workspace) -> Path
    analyze(task, tool_context, workspace) -> Path
    render_calcbook(task, tool_context, workspace) -> Path
    answer_api(task, knowledge_reader) -> Path

适配器负责产生任务产物，不负责自行计算最终分数。

---

## 6. 统一技能、工具和权限

SkillAdapter 只读提供：

    list_skills()
    read_skill(skill_id)
    read_reference(skill_id, relative_path)
    search_cases(query)
    run_skill_script(skill_id, script_id, args)
    skill_bundle_hash()

候选系统只能在本次 workspace 写入，不能修改源技能、参考案例、隐藏测试集、其他架构的运行目录和评价器代码。每个工具必须声明输入输出 schema、读写权限、可访问路径、超时、重试规则和预算计数方式。

正式实验采用无界面调用。OpenCode 或 OSIS UI 可以用于调试“服务未启动、项目未打开、路径错误”等环境问题，但 UI 点击不能成为某一架构的额外能力。

---

## 7. 任务线评价指标

### 7.1 通用指标

所有任务线记录：

- 端到端耗时；
- 输入、输出和推理 token；
- 模型/API 调用次数；
- 工具调用次数；
- 重试次数；
- 超时、资源费用和失败码；
- 运行产物完整性；
- 重复运行一致性。

内部迭代轮数不单独作为质量分，统一反映在耗时、token、工具调用和重试中。

### 7.2 建模线

- 代码编译通过率；
- 标准工程结构完整性；
- `model_conformance` CLI 模型构造正确性；
- PyOSIS 实际模型状态可读性；
- 求解收敛（仅在显式启用 solve gate 时纳入完整成功）；
- 单位、边界、荷载和施工阶段正确性；
- 完整成功率；
- 模块修改的目标准确率和非目标误修改率。

CLI 模型构造正确性和 PyOSIS 实际状态最高优先级。代码静态分数高但模型未构造，不能计为完整成功；未启用求解 gate 时不因“未求解”否定建模质量。

### 7.3 验算修复线

- NG 定位准确率；
- 修复通过率；
- 目标字段修改准确率；
- 非目标模块破坏率；
- 修复后模型一致性；
- 修复耗时和资源。

### 7.4 结果分析线

- 数值提取准确率；
- 单位准确率；
- 控制工况/阶段识别准确率；
- 工程判断正确率；
- 证据和来源完整性。

### 7.5 计算书生成线

- 字段填充正确率；
- 章节、段落和表格结构还原度；
- 书中数值与项目数据一致性；
- 书中数值与模型结果一致性；
- 缺失数据显式标记率；
- DOCX/PDF 渲染完整性。

### 7.6 API 问答线

- 接口和参数用法准确率；
- 示例可执行率；
- 幻觉率；
- 前置条件完整率；
- 响应时间。

---

## 8. 综合分数与完整成功

每条任务线单独设置并冻结权重。建模线建议：

| 维度 | 权重 |
|---|---:|
| `model_conformance` CLI 模型构造正确性 | 40% |
| PyOSIS 执行与模型状态可读性 | 20% |
| 结构字段完整性 | 15% |
| 单位/边界/荷载正确性 | 10% |
| 验证与数值合理性 | 10% |
| 过程可追溯性 | 5% |

综合分计算为各归一化组件乘权重后求和，再乘 100。其他任务线把任务专属正确性设为 50% 以上，其余由证据、结构、耗时、资源和可追溯性组成。

完整成功条件：

| 任务线 | 完整成功 |
|---|---|
| 建模 | 工程编译、PyOSIS 实际模型、CLI 评分、约束/状态验证和产物齐全；求解仅在 solve gate 开启时要求 |
| 验算修复 | 所有 NG 定位正确，修复后验算通过，非目标结构未破坏 |
| 结果分析 | 数值、单位、工况、判断和证据全部正确 |
| 计算书生成 | 字段正确、结构可打开、数模一致、无编造数据 |
| API 问答 | 用法正确、示例有效、无幻觉、前置条件完整 |

---

## 9. 两条对比轨道

### 9.1 architecture-only 主轨

主轨只允许编排方式不同，以下内容必须一致：

- 模型提供商和模型版本；
- 技能、案例和文档内容；
- 工具 schema、权限和文件可见范围；
- OSIS/PYOSIS 执行环境；
- 超时、token 和工具调用预算；
- 验证器、容差、评价权重和隐藏测试集。

### 9.2 native-best-practice 副轨

允许框架使用自身最佳实践，例如 OpenHands 原生技能加载、CrewAI 原生知识源、LangGraph middleware、smolagents prompt template 和 OSIS 原生增量状态。副轨解释为系统级效果，不能单独作为纯架构因果结论。

---

## 10. 统一运行流程

    读取任务 JSON
      ↓
    创建隔离 workspace
      ↓
    挂载同一技能/文档/案例快照
      ↓
    启动对应框架适配器
      ↓
    在预算和超时内自动循环
      ↓
    提交任务线产物
      ↓
    执行任务线专属评价器
      ↓
    保存 trace、日志、快照、分数和 manifest

评测协议是“一次输入、全程不干预”。任务发出后由系统自行循环、报错、修改和重试；重复运行时使用独立 seed，不能人工挑选中间过程。

建模线使用 P0–P6：

| 阶段 | 含义 | 主要产物 |
|---|---|---|
| P0 | 需求解析、桥型识别、任务边界 | plan.json |
| P1 | 坐标、单位、材料和建模假设 | 假设记录 |
| P2 | 截面、节点、单元、边界、荷载、阶段 | candidate_project |
| P3 | 编译候选工程并通过 PyOSIS 执行 `prep/main.py` | build_status.json、执行日志 |
| P4 | `OSISEngine.model_summary()` 状态探针；可选求解 gate | model_state.json、backend_status.json |
| P5 | 调用已有 `model_conformance` CLI 评分 | model_score.json/md |
| P6 | 结果整理和快照 | trace、evaluation、manifest |

其他任务线也记录 P0–P6，但将阶段映射到问题解析、数据定位、修复/生成、验证、回归和报告整理。

---

## 11. 运行目录与 manifest

运行目录：

    runs/<task_line>/<task_id>__<architecture_id>__seed<seed>/
    ├─ input.json
    ├─ adapter_request.json
    ├─ skill_index.json
    ├─ skill_bundle.md
    ├─ skill_mount.json
    ├─ candidate_project/
    ├─ build_status.json
    ├─ model_state.json
    ├─ model_score.json / model_score.md
    ├─ backend_status.json
    ├─ timer.json / execution_trace.json
    ├─ result.json
    ├─ answer.json
    ├─ calcbook.docx/pdf
    ├─ compile.json
    ├─ static_conformance.json
    ├─ trace.json
    ├─ evaluation.json
    └─ manifest.json

所有运行必须记录 task_id、task_line、architecture_id、framework_version、commit、model_snapshot、skill_bundle_sha256、tool_schema_version、solver_version、container_digest、seed、`timeout_s`（对应 `total_timeout_s`）、`subtask_timeout_s`、token_budget、tool_call_budget、artifact_hashes、failure_code 和 human_intervention。

建议失败码：

    task_invalid
    skill_mount_failed
    pyosis_disabled
    pyosis_build_failed
    pyosis_state_unavailable
    pyosis_solve_failed
    candidate_missing
    layout_incomplete
    compile_failed
    static_conformance_failed
    model_build_failed
    solver_not_converged  # 仅 solve gate 开启时
    validation_failed
    result_extraction_failed
    calcbook_render_failed
    api_hallucination
    artifact_incomplete
    timeout

---

## 12. 实验矩阵

### 12.1 建模线

| 因素 | 设置 |
|---|---|
| 桥型 | 6 种 |
| 任务形态 | 整桥生成、单模块生成、模块修改 |
| 难度 | L1/L2，后续增加 L3 |
| 重复 | 2 个随机种子 |
| 架构 | T1–T6 |
| 运行数 | 18 个任务 × 2 seeds × 6 系统 = 216 次 |

### 12.2 其他任务线

每条任务线先建立 6–12 个 dev 任务，覆盖简单、常见和跨模块场景；适配器稳定后扩展 hidden 和 transfer 集。每条任务线都必须让 T1–T6 全部运行，不能只在建模线比较系统。

数据划分：

- dev：允许调试适配器和工具链；
- hidden：正式实验前冻结，答案和评分规则不暴露；
- transfer：跨桥型、跨模板、跨任务线迁移。

---

## 13. 消融实验

消融对象是 OSIS-AI 上增加的组件：

| 编号 | 消融条件 | 主要任务线 | 检验问题 |
|---|---|---|---|
| A1 | 不挂 SKILL | 五条线 | 领域知识值多少分 |
| A2 | 技能不分层 | 建模、修复、API 问答 | 分层是否优于扁平堆放 |
| A3 | 关闭案例池 | 建模、修复、计算书 | 案例是参考还是抄答案 |
| A4 | 固定单模型 | 五条线 | 自动路由节省多少资源 |
| A5 | 换回原生命令 | 建模、修复 | PYOSIS 封装值多少分 |
| A6 | 关闭自动验证 | 五条线 | 验证闭环贡献多少 |
| A7 | 关闭增量重跑 | 建模、修复 | 状态化和局部更新节省多少 |

每次只改变一个因素，其他版本、模型、技能、工具、预算和任务保持不变。

---

## 14. 统计分析和优势声明

以任务为独立单位、seed 嵌套在任务内，二元成功率使用 logistic GLMM：

    success ~ architecture + task_line + task_form + difficulty
              + (1 | task_id) + (1 | seed)

连续分数、耗时、token 和工具调用次数使用 LMM、Gamma 混合模型或对数变换后的混合模型。报告均值、中位数、IQR、95% CI、效应量、任务聚类 bootstrap 和多重比较校正。

跨任务线汇总时，先按每条线的权重得到 0–100 分，再进行总体概览。论文主结论必须同时报告每条线的结果。

只有同时满足以下条件，才表述“在本实验条件下 T6 具有明确优势”：

1. 预注册主要指标支持优势；
2. hidden 和 transfer 集方向一致；
3. 优势不是单一任务线、桥型或 seed 驱动；
4. 消融显示优势与 OSIS 具体组件相关；
5. 建模/修复任务的真实模型或验算结果也支持，而不只是代码分数更高。

---

## 15. 实施顺序与当前状态

实施顺序：

1. 冻结五条任务线 schema、版本、模型、工具权限和预算；
2. 建立五条线的 dev、hidden、transfer 任务；
3. 完成每条线的自动评价器；
4. 先用统一 PyOSISAdapter 打通建模线真实执行和 CLI 评分；
5. 接入 T1 one-shot 基线；
6. 接入 T2/T3/T4/T5，统一交付任务产物；
7. 依次接通验算修复、结果分析、计算书生成和 API 问答；
8. 运行小矩阵，检查失败码、日志和 manifest；
9. 冻结权重和容差，运行正式矩阵；
10. 运行 A1–A7 消融并做统计分析。

当前状态：

| 能力 | 状态 |
|---|---|
| 统一任务/架构元数据 | 已实现基础版本 |
| 只读技能适配器 | 已实现 |
| 建模候选工程物化 | 已实现 |
| 建模标准 py/prep 布局检查 | 已实现 |
| 建模 Python 语法检查 | 已实现 |
| 主项目 `model_conformance` CLI 评价 | 已接通 |
| 建模单次 runner 和 manifest | 已实现 |
| PyOSISAdapter（建模/状态探针/日志） | 已实现，需 OSIS 服务运行 |
| CLI 评分包装和 `model_score.*` 工件 | 已实现 |
| T1–T5 真实框架调用 | 待配置各框架执行器 |
| OSIS 服务端到端冒烟 | 待在运行环境启动服务验证 |
| 验算修复评价器 | 待实现 |
| 结果分析评价器 | 待实现 |
| 计算书生成评价器 | 待实现 |
| API 问答评价器 | 待实现 |
| 五条线正式矩阵 | 待执行器和评价器全部接通 |

---

## 16. 验收标准

正式实验前必须满足：

- [ ] 五条任务线都有 schema、dev 集、hidden 集和自动评价器；
- [ ] T1–T6 都能接收同一任务输入；
- [ ] 建模/修复、结果分析、计算书和问答都有明确产物合同；
- [ ] 技能和案例目录只读；
- [ ] 每次运行都有独立 workspace、trace、evaluation 和 manifest；
- [ ] 耗时、token、工具调用、重试和子智能体计入预算；
- [ ] 真实模型/验算状态与静态分数分开记录；
- [ ] 正式运行不依赖 UI 手工点击；
- [ ] whole/module/modify 建模任务都能运行；
- [ ] 模块修改能统计目标准确率和非目标误修改；
- [ ] 计算书能检查字段、结构、渲染和数模一致性；
- [ ] API 问答能检查签名准确率和幻觉率；
- [ ] dev、hidden、transfer、消融和统计分析可复现；
- [ ] 只有满足预注册规则时才做 T6 优势声明。

## 17. 参考文献与项目

- Liang et al., arXiv:2504.09754
- Google FEABench
- ReAct, arXiv:2210.03629
- CodeAct, arXiv:2402.01030
- OpenHands SDK 与 Agent Skills
- SWE-agent, arXiv:2405.15793
- AutoGen, arXiv:2308.08155
- Memp, arXiv:2508.06433
- ExpeL, arXiv:2308.01340
- AWM, arXiv:2409.07429
- FrugalGPT, arXiv:2305.05176
- CARROT, arXiv:2502.03261
- CADCodeVerify, arXiv:2410.05340
- ModSolAgent, IEEE TII 2026
