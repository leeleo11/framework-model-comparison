# OSIS-AI 与主流智能体框架

## 建模流程对比实验框架设计文档

**文档版本：** v0.1（设计稿）  
**日期：** 2026-09-02　|　**适用任务线：** 桥梁数值建模（第一阶段）

**OSIS Skill Enhance 项目**

## 1. 文档目的与范围

本设计用于比较 OSIS-AI（T6）与表格中五类代表性系统（T1–T5）在桥梁建模任务上的端到端能力。第一阶段只评测建模任务线：自然语言需求 → 候选 PYOSIS/Python 工程 → PyOSIS 实际建模 → `model_conformance` CLI 评分 → 可追溯产物。求解可作为单独 execution gate，不作为建模质量主分数。

附件《工作一实验计划总纲》只作为实验规格参考。其核心约束包括统一模型、工具、案例和资源预算，覆盖整桥建模、单模块生成、模块修改三类任务，并以软件执行后的真实模型构造正确性为最高优先级；附件没有指定必须使用某个外部框架，也不替代本设计中的版本锁定和执行协议。

本设计的目标不是证明任何框架在所有场景下绝对优越，而是在可复现条件下回答：

- 不同智能体编排方式对桥梁模型构造成功率和质量的影响是什么？
- 优势主要出现在需求解析、模块编排、求解验证、错误修复还是增量重跑阶段？
- OSIS 的技能分层、状态化模型、验证器和增量重跑是否带来可测量收益？

## 2. 参照系统与版本锁定

T4 是最适合作为“直接挂载现有技能树”主基线的外部框架。T2、T3、T5 通过统一适配器也可以挂载同一套技能和文档。T1 不视为完整框架，而作为一次性生成基线。

| 系统 | 推荐实现与版本 | 技能/文档挂载 | 建模流程定位 |
|---|---|---|---|
| T1 直接执行 | [Liang et al., arXiv:2504.09754](https://arxiv.org/abs/2504.09754)；参考 [FEABench](https://github.com/google/feabench) | 运行前生成固定技能索引和文档包；不允许交互式读取 | 一次生成脚本并执行 |
| T2 ReAct | [LangGraph](https://github.com/langchain-ai/langgraph) 1.2.11 + LangChain 1.3.18 | `system_prompt` 注入索引；工具读取技能和参考资料 | 思考 → 工具 → 观察 → 行动循环 |
| T3 CodeAct | [smolagents](https://huggingface.co/docs/smolagents/main/reference/agents) v1.26.0 | `instructions`、prompt template 和自定义工具 | 直接生成 Python 动作代码 |
| T4 完整 CodeAct | OpenHands SDK v1.44.1 | 原生支持 `SKILL.md`、`load_skills_from_dir()`、`AgentContext(skills=...)` 和 `.agents/skills/`；参考 [OpenHands 技能指南](https://docs.openhands.dev/sdk/guides/skill) | Agent、Conversation、Workspace 和工具协同 |
| T5 角色多智能体 | CrewAI 1.15.18 | 主轨采用统一技能工具；副轨使用 `knowledge_sources`、Agent、Task、Flow；参考 [CrewAI 文档](https://docs.crewai.com/) | 规划者 → 建模者 → 验证者 → 报告者 |
| T6 OSIS-AI | 正式实验前建立隔离 OSIS 快照 | 挂载 `checkpoints/train-all/skills`，并显式注入安装包 `.agents/AGENTS.md` | OSIS 原生路由、状态、模板和增量重跑 |

## 3. 总体架构

实验系统由四层组成：

```text
任务输入层（同一 JSON）
    ↓
框架生成层（T1–T6，只写 candidate_project）
    ↓
统一 SkillAdapter（同一只读 SKILL/文档/工具）
    ↓
统一 PyOSISAdapter（真实建模 + model_summary）
    ├─ 已有 model_conformance CLI → model_score.json（源码主评分）
    │                              → evaluation.json（现有综合结果）
    └─ manager.all() 详细快照 → runtime_measurements.json
                              → canonical 参数 → 父仓库桥型评分规则
                              → runtime_score.json/.md（运行态旁路评分）
```

建议在独立目录中实现：

```text
<comparison-repo>\
├─ common\
│  ├─ skill_adapter.py      # 只读技能/文档挂载
│  ├─ pyosis_adapter.py     # 唯一真实执行边界
│  ├─ cli_scorer.py         # 调用父仓库已有 CLI
│  ├─ evaluator.py          # 汇总执行证据与主分数
│  └─ task_schema.py        # 任务及限时协议
├─ baselines\
│  ├─ t1_direct\
│  ├─ t2_langgraph\
│  ├─ t3_smolagents\
│  ├─ t4_openhands\
│  └─ t5_crewai\
├─ tasks\
│  ├─ dev\
│  └─ hidden\
├─ runs\
└─ manifests\
```

### 3.1 SkillAdapter

所有系统统一提供以下只读能力：

```text
list_skills()
read_skill(skill_id)
read_reference(skill_id, relative_path)
search_cases(query)
run_skill_script(skill_id, script_id, args)
skill_bundle_hash()
```

适配规则：

- 六个架构都收到同一个 `skill_bundle.md` 和同一个 `skill_bundle_sha256`；`mounting_mode` 只记录框架如何读取，不改变可见内容。
- 技能、参考案例和文档目录只读，框架只能在自己的 workspace 写候选工程。
- T1–T5 框架层不调用 PyOSIS；T6 可以在自己的隔离原生项目中按 `AGENTS.md` 做正常 L0/L1/L2 局部验证，但不读取评分结果、不修改 `runs/`，候选工程提交后仍由统一 runner 接管正式执行和评分。

### 3.2 PyOSISAdapter（统一真实执行器）

```text
create_model(run_dir)
execute(candidate_project, run_dir)
solve_model(run_dir)              # 仅在 --solve-gate 时启用
inspect_model_state(run_dir)      # OSISEngine.model_summary()
validate_model(run_dir)
snapshot_artifacts(run_dir)
```

T1–T6 都把候选工程交给同一个 PyOSISAdapter。T6 的差异只体现在生成阶段是否使用 OSIS 原生路由、状态管理和增量重跑；不得更换执行器或评分器。适配器写入 `backend_status.json`、`build_status.json` 和 `model_state.json`，执行失败或服务不可用必须显式记录。

## 4. 两条实验轨道

### 4.1 architecture-only 主轨

主轨只允许编排机制不同，以下内容必须一致：

该轨道用于分析路由、状态、验证、修复和增量重跑等架构机制。

### 4.2 native-best-practice 副轨

允许各框架使用自身最佳实践，例如 OpenHands 原生技能加载、CrewAI 原生知识源、LangGraph middleware 和 smolagents prompt template。该轨道反映实际系统效果，但结果只能解释为“系统级效果”，不能单独作为纯架构因果结论。

## 5. 统一任务和 I/O 协议

### 5.1 任务形态

每种桥型包含三类任务：

- **整桥生成：** 从自然语言需求生成完整模型。
- **单模块生成：** 只生成指定建模模块。
- **模块修改：** 基于已有模型快照修改一个或多个字段。

难度分为 L1、L2、L3。首轮试验覆盖 L1/L2，完整实验再加入 L3。

### 5.2 输入

```text
input.json
  ├─ natural_language_requirement
  ├─ bridge_type
  ├─ task_form
  ├─ difficulty
  ├─ total_timeout_s
  ├─ subtask_timeout_s         # P0–P6
  └─ initial_project_snapshot   # 仅模块修改任务使用
```

### 5.3 输出

```text
run_dir/
├─ input.json / adapter_request.json
├─ skill_index.json / skill_bundle.md / skill_mount.json
├─ candidate_project/py/prep/...
├─ build_status.json / build_stdout.log / build_stderr.log
├─ model_state.json
├─ model_score.json / model_score.md
├─ runtime_measurements.json
├─ runtime_score.json / runtime_score.md
├─ backend_status.json
├─ timer.json / execution_trace.json
└─ evaluation.json / manifest.json
```

`model_state.json` 必须从软件实际构造后的模型导出，不能仅根据生成代码或文本相似度评分。

运行态文件是源码主评分之外的独立后处理证据：`runtime_measurements.json` 保存
PyOSIS 执行后各 manager 的详细记录；`common/runtime_measurements.py` 将其转换为
与父仓库评分器一致的 51 个 canonical 参数；`common/runtime_scorer.py` 复用父仓库
`SCORERS` 逐维度、逐评分项计算 `runtime_score.json/.md`。运行态缺失字段保持
`None` 并列入 `missing_params`，不得从任务目标或隐藏 reference 补齐。该旁路不改写
`model_score.json`、`reference_score.json` 或 `evaluation.json`，不存在快照时只记录
`not_available`，因此不会改变当前源码评分主实验。

## 6. 统一建模流程和检查点

| 检查点 | 统一含义 | OSIS 内部阶段映射 |
|---|---|---|
| P0 | 需求解析、桥型识别和任务边界 | `_1` 控制 |
| P1 | 建模假设、坐标、单位和材料假设 | `_2` 几何/属性、`_3` 材料 |
| P2 | 截面、节点、单元、边界、荷载和施工阶段 | `_4`–`_8` |
| P3 | 编译候选工程并通过 PyOSIS 执行建模脚本 | `prep/main.py` |
| P4 | 读取 `OSISEngine.model_summary()`；可选求解 gate | `pyosis` |
| P5 | 调用已有 `osis-auto-testconformance` CLI 评分 | `model_conformance` |
| P6 | 结果整理、模型快照和可追溯报告 | `_10` 施工阶段/产物 |

每个框架都要记录 P0–P6 的阶段状态、输入输出、耗时和失败原因。T1 可以只有一次完整调用，但仍需把其产物映射到这些检查点。

### 6.1 两级任务限时器

每个任务同时受总任务时限和分任务时限约束。总任务时限 `total_timeout_s` 从任务下发前开始，到 P6 产物和验证结果写入结束；LLM 调用、工具调用、重试、子智能体、代码编译、OSIS 建模/求解和验证均计入同一墙钟预算。P0–P6 每个阶段另有 `subtask_timeout_s` 硬上限，阶段实际可用时间取分任务预算与总任务截止时间剩余值的较小者，不能在阶段切换时重置总预算。

建议在任务或实验配置中显式写入：

```yaml
total_timeout_s: 5400
subtask_timeout_s:
  P0: 120
  P1: 180
  P2: 600
  P3: 180
  P4: 480
  P5: 180
  P6: 60
```

同一难度等级的任务和所有架构使用同一套预算；L1/L2/L3 可以预注册不同的预算档位，但不得按框架单独放宽。一个阶段内的并行调用、重试和子智能体共享该阶段预算。关键阶段（P0–P5）分任务超时后，直接将任务标记失败；只有协议明确允许恢复时，才可在总任务剩余时间内有限重试。总任务超时则立即停止整条流水线。

超时运行的 `complete_success` 必须为 `false`；已生成代码仍可保留 CLI 分数用于诊断，但 PyOSIS 执行证据和验证分记为 0，并计入失败样本。求解未启用时不添加 `solver_not_converged` 失败原因；启用 `--solve-gate` 时才将其作为额外 gate。所有运行记录 `total_timeout_s`、`subtask_timeout_s`、`timeout_scope`、`timeout_stage`、`elapsed_s` 和各阶段起止时间。

## 7. 首轮建模试验矩阵

建议先进行适配器一致性测试，再运行小规模正式试验：

现有约 95 个模板目录可作为开发集和案例池。隐藏测试集应使用参数扰动、邻近桥型组合、跨模块耦合、预应力/多工况和施工阶段变化，避免模型只记忆模板。

适配器通过后扩展完整矩阵：

```text
6 桥型 × 3 任务形态 × 3 难度 = 54 个任务单元
```

| 因素 | 首轮设置 |
|---|---|
| 桥型 | 6 种：cantilever box、precast small box、precast T girder、rigid frame、conventional box、hollow slab |
| 任务形态 | 整桥生成、单模块生成、模块修改 |
| 难度 | L1/L2，18 个任务中均衡覆盖 |
| 重复 | 2 个随机种子 |
| 系统 | T1–T6 |
| 总运行数 | 18 个任务 × 2 seeds × 6 系统 = 216 次 |

## 8. 评价指标

### 8.1 主要终点：完整成功率

一次运行只有在以下条件全部满足时才计为完整成功：

1. `candidate_project` 标准文件齐全且 Python 编译通过；
2. PyOSIS 建模执行成功，并能从 `OSISEngine.model_summary()` 读到非空模型状态；
3. `model_conformance` CLI 返回可解析分数且达到预注册阈值（建议 0.80）；
4. 单位、边界、荷载等基础状态验证通过，运行工件和 manifest 齐全；
5. 只有命令行显式启用 `--solve-gate` 时，才额外要求求解收敛。

### 8.2 连续质量分数

建议预注册以下 0–100 分权重：

| 维度 | 权重 |
|---|---:|
| `model_conformance` CLI 模型构造正确性 | 40% |
| PyOSIS 执行与模型状态可读性 | 20% |
| 结构字段完整性 | 15% |
| 单位/边界/荷载正确性 | 10% |
| 验证与数值合理性 | 10% |
| 过程可追溯性 | 5% |

### 8.3 过程指标

记录生成耗时、PyOSIS 执行耗时、源码 CLI 评分耗时、token、工具调用数、重试次数和各
阶段超时状态。过程指标用于解释失败和成本，不改变源码主分。

### 8.4 执行后运行态参数评分（旁路）

在 PyOSIS 成功执行后，`PyOSISAdapter` 通过独立 probe 调用各 manager 的 `all()`，
把实际构造出的节点、单元、截面轮廓、材料、边界、荷载工况、施工阶段、钢束和
element group 等记录保存为版本化 `runtime_measurements.json`。这份快照是运行态
评分的唯一数据源，不能用生成源码、任务目标或隐藏 reference 补齐候选值。

`common/runtime_measurements.py` 将快照归一化为与父仓库相同的 canonical 参数集合
（当前 51 个键，覆盖梁高、跨径、截面厚度/面积、材料等级、预应力配置、节点/单元/
边界/阶段等），再由 `common/runtime_scorer.py` 直接复用父仓库
`src/evaluation/levels/model_conformance` 的桥型 `SCORERS` 和报告生成器，保留
D1–D6 维度及其 `items` 逐项分数。结果写入 `runtime_score.json` 和
`runtime_score.md`，缺失值写入 `missing_params`。

该运行态分只作为后期模型真实性审计和补充分析：

- `model_score.json/.md` 仍是当前源码 CLI 主指标；
- `reference_score.json`、`evaluation.json.quality_score` 和源码汇总矩阵不被覆盖；
- 快照不可用时只记录 `not_available`/`extraction_error`/`score_error`，不阻塞原有源码链；
- Excel 额外输出 `运行态对比`、`运行态逐项评分`，与 `框架对比` 的源码矩阵分开统计。

## 9. 公平性和可复现性

每次运行必须自动生成 manifest，至少包含：

```text
task_id
architecture_id
framework_version
commit
model_snapshot
skill_bundle_sha256
tool_schema_version
solver_version
container_digest
seed
timeout_s
token_budget
tool_call_budget
artifact_hashes
failure_code
human_intervention
```

所有 LLM 调用、并行调用、工具调用、重试和子智能体都计入预算；禁止人工修改脚本、补参数或选择性重跑。正式实验采用公开开发集和隐藏测试集分离，实验前冻结权重、容差、超时、随机种子和最小重要差异 Δ。

统计分析建议以任务为独立单位、seed 嵌套在任务内：二元成功率使用 logistic GLMM，连续分数和耗时使用 LMM 或 Gamma 混合模型，报告架构效应、95% CI、任务聚类 bootstrap 和多重比较校正。只有当 OSIS 相对基线的 95% CI 下界超过预注册的 Δ，才表述为“在本实验条件下具有明确优势”。

## 10. 消融和迁移实验

其中迁移实验用于区分“架构贡献”和“技能、工具、模板或求解器优势”。简单一步任务作为负对照，用于检验 OSIS 的优势是否主要出现在复杂建模流程。

## 11. 实施顺序与当前状态

本设计稿只改造 `osis-framework-comparison`；父仓库仅作为只读的 PyOSIS 环境和 CLI 评分来源。正式实验前必须记录 OSIS commit、容器 digest、求解器版本、模型快照、技能包哈希和工具白名单。

## 12. 参考链接

- [Liang et al., arXiv:2504.09754](https://arxiv.org/abs/2504.09754)
- [Google FEABench](https://github.com/google/feabench)
- [LangGraph](https://github.com/langchain-ai/langgraph)
- [LangChain create_agent API](https://reference.langchain.com/python/langchain/agents/factory/create_agent)
- [smolagents Agents API](https://huggingface.co/docs/smolagents/main/reference/agents)
- [smolagents Building good agents](https://huggingface.co/docs/smolagents/tutorials/building_good_agents)
- [OpenHands Agent Skills & Context](https://docs.openhands.dev/sdk/guides/skill)
- [OpenHands skills overview](https://docs.openhands.dev/overview/skills)
- [CrewAI documentation](https://docs.crewai.com/)
## T2 接入状态（2026-09-03）

T2 已接入真实 LangGraph `create_agent` ReAct 生成器，代码位于
`baselines/t2_langgraph/adapter.py`，入口为 `scripts/run_t2.py`。生成层提供
技能索引/技能正文/参考资料检索、候选文件读写和工程完整性检查工具；写入范围限制
在本次运行的 `candidate_project` 内。

T1–T6 使用统一模型 `deepseek-v4.1-flash-expires-on-0910` 和 OpenAI-compatible 接口
`http://47.92.150.231/v1`。生成阶段通过 `ExperimentRunner.candidate_generator`
纳入总任务时限；生成完成后仍由同一个 PyOSISAdapter 和
`osis-auto-testconformance` CLI 执行、取模型状态和评分。T2 不需要 OpenCode 4096，
但必须保持 OSIS 18080 服务运行。API 密钥只从 `OSIS_MODEL_API_KEY` 或
`OSIS_API_KEY` 环境变量读取。

当前验证已覆盖：LangGraph 工具调用往返、候选目录越界写保护、生成元数据脱敏、统一
运行器的生成阶段计时，以及 `deepseek-v4.1-flash-expires-on-0910` 的真实工具调用。正式运行命令见项目
`README.md` 的 “T2 LangGraph ReAct” 小节。
